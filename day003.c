#include <stdio.h>

int main(void) {
  float number1, number2;
  scanf("%f%f", &number1, &number2);
  printf("Multiplication result is %f\nDivision result is %f", (number1 * number2), (number1 /number2));
  return 0;
}
