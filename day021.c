#include <stdio.h>

int main(void) {
  
  for(int numbers = 1; numbers<10; numbers++){
    printf("%d", numbers);
    if(numbers == 1){
      printf("\n");
    }else if(numbers == 4){
      printf("\n");
    }
  }
  
  return 0;
}
